### Feature

**You own the design. Plan, review, verify.** Keep implementation ownership and review separate without inventing delegation that the active spawn policy forbids.

The parent copies these ordered steps into its todo before task-specific items. Keep omitted steps with `skip: <reason>`. Children return evidence and status, not a second todo. Read and apply every named `skill://` dependency before using it. A user pause on runtime validation applies to every agent and every verification gate below. Record the deferred gate and do not claim it passed.

1. Read and apply `skill://how` over the affected subsystem.
2. Read and apply `skill://architect` for parallel design exploration. A justified omission stays as `architect skipped: <reason>`. Do not fold the design decision silently into implementation.
3. Write the throughput checkpoint as four parent-owned todo items. Keep a dimension that genuinely does not apply, such as a single file or no fan-out, with `n/a: <reason>`.
   - **Blocking first steps.** Resolve prerequisite contracts and permitted gates before fan-out. Children do not run mid-flight validation against siblings' partial edits.
   - **Independent workstreams.** Disjoint files, services, or layers parallelize. Shared writes serialize.
   - **Shared mutable state.** Read and apply `skill://principle-separate-before-serializing-shared-state`. Split the target first. Serialize only for real invariants.
   - **Smallest safe decomposition.** If one owner is best, name why. Inline scoping comes first. Fan out only genuine independent substantial slices in one batch, not one trivial task or artificial padding.
4. Implement the named data shape with explicit code ownership. Read and apply `skill://principle-model-the-domain` before logic. Choose a state machine over scattered booleans, a table or registry over branching, or a typed model over repeated shape assumptions. For permitted substantial independent slices, use native `task` with `isolated: true`, exact paths, the chosen structure, and observable success criteria. Select the configured general task agent for reasoning and implementation, `sonic` only for mechanical edits, and `reviewer` for independent review. Do not pass a model. The lead reviews the actual changes and owns the summary.

   When implementation admits multiple valid shapes, such as error handling, abstraction layers, or test structure, read and apply `skill://arena` so alternatives and cross-judgment inform the choice. Keep competing implementations unapplied until selection using arena's isolated no-apply workflow or read-only proposals. Native task isolation may auto-apply patches and is not sufficient to keep competing versions separate. The purpose is review separation, not lines saved. Small application size alone does not remove the review requirement. If spawn, recursion, plan-mode, or tool limits prohibit the requested arrangement, disclose that limit, implement inline when allowed, and retain an explicit independent review gate where available. Never fabricate a child or bypass policy with recursive orchestrator calls. Do not yield a standing-by reply while work remains.

   Give a batch shared `# Goal`, `# Constraints`, and `# Contract` context and each child `# Target`, `# Change`, and `# Acceptance`. Include required skill paths, upstream source pointers, file ownership, and the instruction to skip tests, builds, lint, formatters, and runtime validation until integration. Children have no conversation history. The parent keeps a scoped `local://` notepad and consumes `agent://<id>` outputs. Use known `hub` IDs for follow-up and `history://<id>` only for known task sessions. Do not assume an isolated completed worker can be revived.

   Make surgical edits. Re-ground upstream-derived files against their source. Use LSP definitions and references before semantic migrations, preview supported rename or code actions before applying, and use `ast_edit` for structural codemods. Port shared-primitive improvements to every consumer and include each in the later verification scope. Follow the Comments rules in `skill://sky-mode`. Commit only within the user's authorized repository workflow.
5. Verify on the matching surface. Read and apply `skill://control-ui` for browser or Electron work and `skill://control-cli` for CLI or TUI work. Open an Eval `browser` tab before web interaction, observe the real page, drive changed paths, and capture visual evidence. Use native `hub` start, readiness, logs, send, and stop for supervised programs. For non-UI APIs, exercise the real consumer path. Inconclusive evidence or the wrong surface is not a pass. Flag it. The parent owns integrated proof rather than trusting a delegate's summary.
6. Shape small ordered commits and stack follow-ups when the user authorized commits and repository workflow. Read and apply `skill://principle-sequence-verifiable-units`. Build and verify each coherent unit before the next where prerequisites require it; do not run overlapping checks during concurrent edits. Rebase only within the authorized history scope. If commits are not authorized, preserve the proposed unit order in the handoff instead.
7. If the design is contested, read and apply `skill://interrogate` before shipping. Configured diverse-model panels are optional. Report when available reviewers share a model rather than calling that independent model agreement.
8. Read and apply `skill://sky-mode/playbooks/opening-a-pr.md` only when the requested repository workflow includes opening or updating a PR. Otherwise retain this step with the reason it does not apply. No implicit push, merge, or external publication.

Code-coupled work, one feature or one migration, has one integration owner with the checkpoint inline. That owner fans out only permitted independent slices after the blocking phase. Parent-level fan-out is for independent artifacts, such as audits, cross-subsystem investigations, or competing experiments. Rewrite the checkpoint at phase boundaries. Use known-agent follow-up for a continuing scope; if ownership or scope changes materially, give a fresh owner a consolidated handoff rather than relying on interrupt chains.

**Reply:** what you built, what you chose and why, the throughput checkpoint, and open decisions. Use tables for design alternatives. State the proof and any paused or unavailable gates. Link a PR only if one actually exists and was read or created in this run.
